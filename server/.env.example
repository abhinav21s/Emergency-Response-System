# Paste your free MongoDB Atlas connection string here. Leave unset only if using local MongoDB.
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/ambulance_demo?retryWrites=true&w=majority
PORT=5000
TOMTOM_API_KEY=your_free_tomtom_key
